论文的主要创新点有两个：
(1) 一个集合预测损失，强制预测框与真实标注框之间进行唯一匹配；
(2) 一个架构，（在单次前向传播中）预测一组目标并建模它们之间的关系。

阅读论文过程中提出了如下问题：
1. 如何使用基于匈牙利的二分匹配法强制预测框与真实标注框之间进行唯一匹配？
2. 目标检测集合预测损失分为两部分，类别损失和边界框损失，在代码中是如何体现的？
3. 解码器为什么可以一次并行输出所有目标，而不是按照序列顺序逐个输出？
4. 解码器输入的“一小组固定的学习型目标查询”是如何得到的？在推理时始终是不变的吗？
5. 解码器的目标查询上又加入了一个位置编码，这个编码如何得到的？
6. 在标准transform框架下，序列时位置编码是正弦和余弦编码一个一维的一个位置，现在是图像，二维空间，如何描述一个位置？
7. 位置编码为什么要输入到每个层，而不是只输入到第一层？
8. cnn特征、编码器、解码器注意力权重的图是如何显示出来的？


感悟：
深度学习的设计，模型结构的设计每一步都是和现实运行逻辑相对应的，逻辑通，就更好训练，可以获取更好的结果。
就比如使用使用辅助损失，即在每个解码器层都计算损失可以获得更好的结果，实际上就是考虑的更全面。





